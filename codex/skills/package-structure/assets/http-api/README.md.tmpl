# `__PACKAGE_NAME__`

Effect HTTP API transport over `__DOMAIN_PACKAGE__`. Browser clients use Fetch;
server loaders compose the in-process client and never loop back over HTTP.

Import the client contract from `/client/service`, browser composition from
`/client/browser`, in-process composition from `/client/in-process`, and server
routes from `/server`. Public HTTP changes update the repository API architecture
owner and affected critical journeys/proof in the same slice. Provider operations,
authority, and runbooks remain repository-owned; this package makes no deployment
or external-route claim.
