# Web

TanStack Start application. It owns routing, server adaptation, Effect runtime
composition, SSR encoding/restoration, and runtime disposal. The server runtime
uses the in-process HTTP client; the browser runtime uses Fetch.
