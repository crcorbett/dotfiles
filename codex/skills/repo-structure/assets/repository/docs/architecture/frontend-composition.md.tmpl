# Frontend composition

Routes own cross-feature orchestration and SSR restoration. A leaf owns its
narrow read/command/skeleton/fallback; pure presentation leaves accept narrow
readonly props. Prefer children/slots and colocated feature components. Reject
boolean-prop matrices, synchronization Effects, hooks that rename one call, and
giant route components.
