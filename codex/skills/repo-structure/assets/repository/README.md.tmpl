# `__REPO_NAME__`

Bun/Turbo monorepo with a TanStack Start app and Effect domain, RPC, and HTTP
API packages. Resolved dependency snapshot: `__RESOLVED_AT__`.

```bash
bun install
bun run verification
```

Read `AGENTS.md`, then use [`docs/README.md`](docs/README.md) to load only the
semantic owners needed for the change. Generated route trees, lockfiles, output,
and caches are tool-owned rather than templates. The recorded dependency snapshot
was selected on `__RESOLVED_AT__`; it is not a claim that versions remain current.
