# __REPO_NAME__

Bun/Turbo monorepo with a TanStack Start app and Effect domain, RPC, and HTTP
API packages. Resolved dependency snapshot: __RESOLVED_AT__.

```bash
bun install
bun run verification
```

Read `AGENTS.md` and `docs/architecture/` before changing boundaries. Generated
route trees, lockfiles, output, and caches are tool-owned rather than templates.
